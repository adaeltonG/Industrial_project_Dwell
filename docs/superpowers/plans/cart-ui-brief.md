# Dwell shopping UI brief

Implement functional cart and checkout pages within the existing Next.js app. Preserve existing local changes. Read existing components and globals.css for the established cream/green palette, typography, borders and spacing. Keep the interface restrained and consistent; prioritize clear product rows and a distinct order summary. Mobile layouts must fit 375px screens.

Your owned files: app/cart/page.tsx, app/checkout/page.tsx, components/Header.tsx, components/ProductCard.tsx, app/products/[slug]/page.tsx, and appended shopping styles in app/globals.css. Do not modify other files.

Root supplies components/CartProvider.tsx and components/ProductImage.tsx. useCart() exposes {items: CartItem[], ready: boolean, count: number, addItem(product: Product): string | null, setQuantity(id: string, quantity: number): void, removeItem(id: string): void, clearCart(): void}. CartItem = {product: Product, quantity: number}. addItem returns error text or null on success. lib/cart.ts exports MAX_QUANTITY=99, toMinorUnits(price: string): number, cartTotal(items: CartItem[]): number (minor units). ProductImage accepts {product: Pick<Product,'slug'|'title'|'imageUrl'>, className?: string} and renders image wrapper.

Add cart link with count visible on desktop AND mobile including details page with mobileAction=login. Add-to-cart buttons on cards and details, disabled for OUT_OF_STOCK. Show feedback via accessible status. Cart shows images, linked titles, vendor, unit price, editable quantity 1..99, remove action, subtotal, free simulated delivery and checkout link. Include useful empty/loading states.

Checkout is explicitly a simulation, no card details or real order promises. Collect validated name/email/address/postcode for a demo delivery preview, without persisting personal data. Use required inputs and trim checks. At submit, fetch each current product from existing API /products/{slug}; verify same ID, stock, currency and price; if anything changed, show actionable error and do not clear cart or confirm. On successful simulation, show reference, total, item summary, no-payment message; clear cart only after successful validation. Guard repeated submission and avoid clearing items changed while awaiting requests (disable changes or compare current cart snapshot). API unavailable preserves cart and offers retry. No new payment dependency or backend writes.

Use Next Link (automatic /dwell prefix), existing apiRequest and formatPrice. Include demo order summary on success and continue-shopping link. No external image URLs: use ProductImage. Root owns image generation. Follow C:/Users/adael/.agents/skills/frontend-design/implementation.md for implementation standards. Run frontend tsc when done and report changes and limitations.
